import { GenresLimitPipe } from './genres-limit.pipe';

describe('GenresLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new GenresLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
